export const defaultFakeLoadingOptions = {
  delay: 500,
  minDuration: 200,
};

export const FAKE_LOADING_STATES = Object.freeze({
  IDLE: 'IDLE',
  EXPIRE: 'EXPIRE',
  DISPLAY: 'DISPLAY',
  DELAY: 'DELAY',
});
